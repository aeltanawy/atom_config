import { VegaMediaType } from "./mime";
export interface VegaOptions {
    renderer: "canvas" | "svg";
}
/** Call the external library to do the embedding. */
export declare function embed(anchor: HTMLElement, mediaType: VegaMediaType, spec: Readonly<{}>, options?: Partial<VegaOptions>): Promise<any>;
